apply plugin: "war"

bootWar {
    mainClassName = "com.hc.taskms.TaskMsApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
